# Contributing

Preserve caller-owned lifecycle, exact W3C Web of Things terminology, and the
transport/credential port boundaries. Add normative requirements and vectors
before widening an operation or binding claim. Run `WOTEX_PATH_DEPS=1 mix check`
against the sibling core during coordinated local development.
